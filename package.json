{
  "name": "csc-308-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run -w react-frontend dev",
    "build":"npm run -w react-frontend build",
    "preview": "npm run -w react-frontend preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
  ,
  "workspaces": [
      "packages/*"
  ]
}
